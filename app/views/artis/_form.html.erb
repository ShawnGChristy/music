<%= form_for(@arti) do |f| %>
  <% if @arti.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arti.errors.count, "error") %> prohibited this arti from being saved:</h2>

      <ul>
      <% @arti.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
